<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/movieListFragment">

    <fragment
        android:id="@+id/movieListFragment"
        android:name="com.janewaitara.movieapp.MovieListFragment"
        android:label="fragment_movie_list"
        tools:layout="@layout/fragment_movie_list" >
        <action
            android:id="@+id/action_movieListFragment_to_movieDetailFragment"
            app:destination="@id/movieDetailFragment" />
    </fragment>
    <fragment
        android:id="@+id/movieDetailFragment"
        android:name="com.janewaitara.movieapp.MovieDetailFragment"
        android:label="MovieDetailFragment" >
        <argument
            android:name="movie_title"
            app:argType="string" />
        <argument
            android:name="movie_image"
            app:argType="integer" />
        <argument
            android:name="movie_summary"
            app:argType="string" />
        <argument
            android:name="movie_release_date"
            app:argType="string" />
    </fragment>
</navigation>