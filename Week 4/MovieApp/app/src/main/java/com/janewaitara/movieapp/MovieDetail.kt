package com.janewaitara.movieapp

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import kotlinx.android.synthetic.main.activity_movie_detail.*

class MovieDetail : AppCompatActivity() {
/*
    //property to contain my movie
    lateinit var movieItem : Movie

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_movie_detail)

        movieItem = intent.getParcelableExtra(MainActivity.INTENT_MOVIE_KEY) as Movie
        title = movieItem.title

        movie_image.setImageResource(movieItem.image)
        movie_title.text = movieItem.title
        movie_release_date.text = movieItem.releaseDate
        movie_summary.text = movieItem.summary
    }*/
}
